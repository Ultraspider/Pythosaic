Python program to transform a picture into a mosaic of smaller pictures.

Work in progress

Input is a raw image prepared with ImageMagick's convert:

e.g: convert -resize 20% picture.jpg -depth 8 -colors 32 rgb:rpicture
